# Скопіюйте цей файл у ".env" і задайте свій пароль перед збіркою (npm run build).
# Пароль потрапляє у зібраний JS-файл каси — це проста перепона від випадкових
# відвідувачів URL, а не криптографічний захист.
VITE_APP_PASSWORD=
